# Session 2026-06-20 0330 — Cancel Guaranteed Roll: 2 charges → 1 charge

Per-player Cancel charges reduced from two to **one**, in both modes. (Kept the refund-on-HIT
mechanic: a successful HIT still refunds the charge so you can cast again — just never two pending.)

## Backend (`gameLogic.js`)
- All three init sites set `cancelItems = 1`: `addPlayer` (line ~370), `_resetToLobby` (~1203),
  `completeReveal` (~1286).
- `_cancelChargeMax()` now returns **1** (the refund cap). The three refund sites
  (`_resolveCancelOnRoll` HIT, `makeGhost` target-ghost, `removePlayer` target-leave) are unchanged —
  they use `Math.min(x+1, _cancelChargeMax())`, so they now cap at 1 automatically.
- Updated the cancel doc comments (addPlayer field block, `useCancelItems` header, `_cancelChargeMax`)
  from "two charges / up to two cancels" to "one charge / at most one out at a time; a HIT refunds".
- The can't-stack-on-same-target guard is untouched (still meaningful across two different casters).

## Tests (`test_game.js`) — 1013 → **1012** assertions
- Rewrote the 68c cancel section + the 85i / 96c / 98a cancel blocks for the 1-charge model:
  start `=== 1`; a cast goes `1 -> 0`; a HIT refunds back to `1` (cap 1); a MISS / forfeit stays `0`.
- The old "two charges → second cast on a different player allowed" block became "one charge → second
  cast rejected (out of charges)"; the "two cancels out at once" block became "at most one out at a
  time" (net −1 assertion).
- The 2v2 spread test still passes as-is logically (mate1 casts opp1 `1->0`; mate2 with its own charge
  is rejected on opp1 by the stack guard and stays `1`; mate2 then covers opp2) — only the asserted
  number changed (`mate2.cancelItems === 1`).
- Applied via a counted Node script (asserts each find/replace hit its expected occurrence count) to
  avoid silent mis-edits; the multi-line block rewrites were done with Edit.

## Docs / frontend / memory
- `rules.md` Cancel section: "one charge", "one at a time" (HIT refunds; two different players can each
  have one out).
- `game-items.js` comment "2 charges each" → "1 charge each (a HIT refunds it)". The button text is
  already `me.cancelItems`-driven (no hardcoded number).
- Memory `bananaland-project.md` updated to the 1-charge model.

## Verify
- `node backend/test_game.js` → **1012 passed, 0 failed**. Harnesses 43/43 + 20/20.
- **Live (preview, 3-player socket game):** each player starts with `cancelItems === 1`; the button
  reads "🚫 Cancel Guaranteed Roll (1 left)"; casting on B2 → `0`, button "(0 left)" + **disabled**; a
  second cast on B3 is **rejected** (stays `0`). Zero console errors.
- Adversarial audit workflow (backend logic / tests / docs+frontend / completeness critic, 4 agents)
  came back **clean — zero findings**: every init/cap/refund site, every test assertion, and all
  docs/frontend wording match the 1-charge model; no stale two-charge reference anywhere outside the
  historical `summaries/`.

## Out of scope (flagged separately)
- The audit surfaced pre-existing dead code unrelated to this change: `game-screen.js:965` calls
  `cancelAbilityTargeting()` (defined nowhere) inside an `if (window._abilityTargetMode)` block whose
  flag is never set true → unreachable. Flagged as a background task to remove; not touched here.
