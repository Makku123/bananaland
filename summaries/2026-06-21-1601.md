# Session 2026-06-21 1601 — Rune-chip tooltip simplified to just the quantity

User: "make it so that simply when you hover over your runes on the right hand side of the game, just
the quantity shows on the tool tip, not that other stuff."

Follow-up to the 1542 magic-runes polish: the chip hover tooltip was a multi-section "spell-plaque"
(gradient header + ✧ sigil + big rune numeral + gold count + action hint). User wants only the
quantity held.

## Change
- `game-screen.js` `makeChip`: the `.chip-tip` content is now just `×N` (the count of that rune in
  hand). Dropped the `actionText` computation and the `.chip-tip-head/-body/-val/-meta/-count/-action`
  markup. Kept the `.chip-tip` bubble + `.chip-tip-notch` sibling and the `aria-label`
  (still "magic rune V, N held, armed").
- `styles.css`: `.chip-tip` reworked from a 146px multi-row plaque into a **compact `×N` bubble**
  (auto-width, `padding 4px 11px`, 16px/900, nowrap, purple bubble + glow). Removed the now-unused
  `.chip-tip-head(::before)/-body/-val/-meta/-count/-action` rules (dead CSS). Notch + hover-reveal +
  reduced-motion rules unchanged.

## Verify (screenshots wedged → computed-style + geometry)
Injected the current chip markup into a forced-visible `#magic-dice-list`:
- tooltip text = `×3` (count 3) and `×1` (count 1) — just the quantity, no children, no leftover
  sub-elements; the removed CSS rules are gone (0 found in stylesheets).
- bubble: opacity 0 by default, absolute, 16px, nowrap, z1000, ~42px wide (vs old 146px), centred
  above the chip; `.roll-chip:hover .chip-tip{opacity}` reveal rule intact.
- `node --check` clean; zero console errors.

The flashy cards (holo shine) + gold pulsing `×N` badge from 1542 are unchanged.
