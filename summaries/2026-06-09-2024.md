# Session summary — 2026-06-09 20:24 PT (scheduled run)

## Run-start git note

Same mount artifact as prior runs. The protocol checkpoint commit
(397f7b6) captured corrupt blobs — frontend files with trailing-space
padding, and backend gameLogic.js/test_game.js **truncated mid-line**.
Verified the host files were intact (read via host-side file tools),
removed the corrupt checkpoint (develop pointed back to 98993aa, never
pushed), rebuilt the index via GIT_INDEX_FILE. Stale `.git/HEAD.lock`
and `develop.lock` blocked ref updates until file-delete permission was
granted for the run. This run's commit was again built with git
plumbing from verified-clean /tmp copies (clean `git archive` of HEAD
+ edits); a post-edit check confirmed the mount appends ~1.8KB of NUL
padding to gameLogic.js that does not exist on the host.

## Task: dead-code sweep of gameLogic.js public/private methods

Last run flagged `tradeProperty` as dead. Swept all 129 method names in
gameLogic.js against server.js, test_game.js, and the frontend (game.js,
board.js, plus a repo-wide grep including HTML/shell/JSON to rule out
dynamic dispatch). Five methods are referenced nowhere outside their own
definition; all five removed:

- `tradeProperty` (~40 lines) — 1-for-1 property swap with no socket
  handler and no UI; also absent from rules.md, so removal improves
  rules compliance.
- `_isRevealedToAnyone` — exact logic duplicate of the used
  `_isGenuinelyRevealed`.
- `_globalGenuineRevealed` — unused set-builder next to the used
  `_genuineRevealedGrowPositions`.
- `getTeamBananas` — unused team-total helper.
- `isLobbyReady` — trivial unused getter.

Net: gameLogic.js 5066 → 4998 lines. No behavior change intended.
The live sell-listing system (sellProperty/buySale/cancelSale) was NOT
touched — it still awaits Mark's document-or-remove ruling.

## How it was tested

Game not running on the host → sandbox (`/tmp/run_1781061549`, clean
`git archive` of HEAD + edits):

- `node --check` on gameLogic.js and test_game.js.
- Full backend suite: **517 passed / 0 failed**.
- Server boot (JWT_SECRET set) + HTTP smoke: `/`, `/index.html`,
  `/game.js` all 200.

## Pending / open questions (carried forward)

- **Ghost sealed-bid tie ruling** (19:55 run): tie hands the tile to the
  ghost lander free — confirm intended.
- **Desert rules mismatch** needs Mark's ruling (rules.md "does nothing"
  vs deliberately-buyable in code).
- 10s ghost-concede grace window (16:57 run) still awaits confirmation.
- **Sell-listing system** (sellProperty/buySale/cancelSale) is live in
  2v2 UI but undocumented in rules.md — document it or remove it?
- **Possible 2v2 poker edge case noticed during audit:** rules.md says
  poker doesn't fire when "2+ players are already on the tile", but the
  duel check counts only non-teammate opponents — in 2v2, landing on a
  tile holding your teammate + one enemy still starts poker. Ambiguous
  reading; needs a ruling before changing.
- Mount corruption recurs every run (phantom modifications, NUL padding,
  truncated reads through the mount). Worth investigating outside the
  hourly run; until then every run must commit via plumbing from /tmp.
- **PUSH IS FAILING — needs Mark.** `git push origin develop` fails with
  "could not read Username for 'https://github.com'": no credential
  helper, token, or gh CLI is available in the sandbox. develop was
  already **8 commits ahead of origin/develop at run start** (now 9),
  so earlier runs' work has not reached GitHub either. Fix options: add
  a credential helper / PAT the sandbox can use, or push manually from
  the host (`git push origin develop`).
