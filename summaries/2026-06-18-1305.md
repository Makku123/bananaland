# Run summary — 2026-06-18 13:05 PT

Two requests: (1) rename **"pineapple bomb" → "monkey trap"** everywhere and raise
the trap timer **3 → 7**; (2) an **exhaustive bug audit** of the major systems,
fixing clear bugs and asking about ambiguous gameplay calls.

## Part 1 — Rename + timer (full scope, per user)

User chose **full rename incl. code internals** and the **🪤** emoji.

- Scripted case-aware pass (938 replacements over 19 files) + a follow-up pass:
  `pineapple→monkey`, `bomb→trap` (incl. English inflections `bombed→trapped`,
  `bomber→trapper`, `bombing→trapping`), all identifiers (`this.traps`,
  `trapMode`, `player.trap`, `placeTrap`/`buyTrap`, `_checkTrapDetonation`,
  `_trapEliminate`, `_removeExpiredTraps`, `lastTrapEvent`, `trapWinner`), CSS
  classes, HTML element IDs, getState fields, socket events (`buy_trap`/
  `place_trap`) — kept consistent across backend↔HTML↔CSS↔JS↔tests.
- Emoji 🍍 **and** 💣 → 🪤 (the item used both). **Gotcha:** `board.js` stored the
  pineapple as an UPPERCASE escape `🍍` while the first sweep only caught
  lowercase, so the on-board icon stayed a pineapple until a case-insensitive Node
  pass caught it (3 more). Also swapped the two decorative 🍍 in the `game-init.js`
  background fruit-rain. Title is now `🪤 MONKEY BUSINESS 💰`.
- Timer **3 → 7** at both the placement and arming sites + all prose (rules.md,
  tutorial, in-game help row, board tooltip). Fixed a pre-existing stale help row
  that still described the old blast design.
- **Verify:** 0 residual `bomb`/`pineapple`/🍍/💣 in source or docs; CRLF preserved;
  all JS parses; live preview confirmed every label + the on-board trap render
  (🪤, "Monkey Trap (active — 7 turns left)").

## Part 2 — Audit (Workflow: 12 subsystem finders + adversarial re-verify, 38 agents)

Output: 6 confirmed (1 was a duplicate → **5 distinct bugs**), 15 ambiguous, 5
dismissed. **Fixed all 5 bugs; asked the user about 4 gameplay calls.**

### Bugs fixed (all with regression tests, section 96)
1. 🔴 **HIGH — a ghost (disconnected player) could land on the Super Banana and
   win** (incl. carrying its whole team), via the server's auto-driven rolls. The
   SB block had no ghost guard. Fix: a ghost lander now **relocates** the SB (like
   a broke lander) and can never buy or free-win it.
2. 🟡 **MED — classic item auctions ignored the `noAuctionTimer` host setting**
   (farm + team-item honored it), force-resolving after ~15s. Fix: gated all 3
   classic-item timer sites (respond / sealed / silent tie-break) on
   `noAuctionTimer`.
3. 🟢 **LOW — a Cancel-Items charge was wasted** if the target ghosted after the
   cast. Fix: `makeGhost` refunds + clears a pending cancel on the new ghost
   (mirrors `_trapEliminate`).
4. 🟢 **LOW — rematch re-broadcast stale animation hints**. Fix: `_resetToLobby`
   now nulls `lastStartPick` / `lastSuperBananaSwap`.
5. 🟢 **LOW — hardening:** `endTurn` (+ the auto-end callback) now refuse during a
   live `auction`/`poker` (parity with `notifyAnimsComplete`; prevents an orphaned
   auction / leaked poker pot); `removePlayer` now burns the leaver's cash to keep
   the banana ledger conserved.

### User decisions on ambiguous items
- **Grow double-fire** (roll L + land on GROW L = 2×): **keep** (intended).
- **Start-pick onto an armed trap**: **leave as-is**.
- **Trap-released farm pile double-dip**: user noted (correctly) this is
  unreachable — `_trapEliminate` (the only elimination path) and `removePlayer`
  both remove a dead/leaving player's traps, so a triggering trap always has a live
  placer. **Verified true; no change.**
- **Classic trap-out win** ("last monkey standing"): **keep + reword docs**.
  rules.md already documented it; the **tutorial** was stale → updated it (and fixed
  two other stale tutorial bits found there: "2-4 players"→"2-6", and it still
  advertised the **removed** "Send bananas" feature → now shows 2v2/3v3).

Lower-priority ambiguous items left as-is (reported to user, no action requested):
no rent mechanic (by design), classic sealed-bid tie → earliest bidder, all-broke
2v2/3v3 pitch uses Buy Now, cancel-on-current-roller timing, no auto-concession
when a whole team ghosts out.

## Verification

- **Tests:** added section 96 (10 new assertions) for all 5 fixes. **Full suite:
  1185 passed, 0 failed**, deterministic ×3; oauth passes; all files parse.
- **Adversarial fix review** (5 agents, one per fix, each writing a harness to
  *break* the fix): **5 solid, 0 problems** — ghost-SB held across 500+ targeted +
  300 randomized trials + auto-drive + team + reconnect; endTurn guards introduce no
  soft-lock; removePlayer conserves the ledger; reset enumerated all 25 transient
  fields with none missed.
- **Live (preview):** tutorial Game Modes updated, title 🪤, 0 console errors.

No git operations.
