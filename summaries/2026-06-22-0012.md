# Session 2026-06-22 0012 — Shuffling/reveal screen 15s → 5s

User: "make the shuffling screen 5 seconds not 15."

The reveal ("revealing"/shuffling) phase duration lives in TWO synced spots:
- **Authoritative (backend):** `server.js` start_game handler — `setTimeout(... completeReveal ..., 15000)`
  → **5000**. This is what actually ends the phase (state revealing → playing).
- **Cosmetic (frontend):** `game-socket.js` `_attachRevealCountdown` — `const TOTAL = 15` → **5**, the
  static placeholder `"Memorize the board… starting in 15"` → `…5`, and the function comment `15s` → `5s`.

## Verify
- `node --check` clean (server.js + game-socket.js); backend suite **992 passed / 0 failed** (tests call
  `completeReveal()` directly, so they don't depend on the timer value).
- Live: `_attachRevealCountdown` now renders "Memorize the board… starting in 5" (no "15"); no console errors.
- Confirmed the other `15000`s are unrelated and untouched: auction phase timers (server.js 398/406/408/415)
  and the in-game redraw auto-keep timer (gameLogic.js:1354).

(Memory already stated "reveal auto-completes server-side after 5s" — now matches reality.)
