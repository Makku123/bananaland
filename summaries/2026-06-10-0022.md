# Run summary — 2026-06-10 00:22 PT (scheduled, autonomous)

Run task: rules audit of gameLogic.js + the dead-code cleanup queued by the
22:50/22:51 runs + pinning two audit findings as tests. No git operations.

## Audit result: no new rules violations

A fresh rules.md-vs-gameLogic.js audit surfaced two candidate violations;
both turned out FALSE on verification:

- **Auction pitch cap** (placeBid): the `player.money >= maxOtherMoney` guard
  looked like it skipped the richest-opponent cap for poorer landers — but
  the line above already caps every pitch at the lander's own bank, so a
  pitch can never exceed the cap when the guard is false. Correct, just
  confusingly written.
- **Vine Swing in 2v2**: "cannot swing to an allied farm" is already enforced
  by the stricter own-farms-only target rule (`prop.owner !== player.id`).

## What changed

1. **backend/gameLogic.js** — simplified the redundant pitch-cap guard to
   `if (amount > maxOtherMoney)` + a one-line comment. Provably
   behavior-neutral (see above); removes the trap that fooled the audit.
2. **frontend/board.js** (?v=6) — deleted the unreachable legacy multi-group
   renderer inside `renderPreviewTileList` (~157 lines: Cavendish/Blue Java
   color groups, tax/bus "Other Tiles"). Every layout from
   `buildPreviewLayout` is the single-"farm"-group board, so the dispatcher
   now just clears the panel and calls `renderFarmTileList`.
3. **frontend/auth.js** (?v=3) — removed the duplicate `escapeHtml` (queued
   cleanup). The game-items.js version already won at load time (later
   script, same global scope) and is the safer one — it escapes quotes,
   which auth.js's `src="..."` interpolation relies on.
4. **frontend/index.html** — cache busts for the two files.
5. **backend/test_game.js** — new section 83 (10 asserts): 3-player pitch-cap
   tests (cap = richest opponent; own bank binds for poorer landers) and
   Vine-Swing-to-teammate's-farm rejected / own farm still works. Also
   updated the section-19 assert "Bomb cost defaults to 666" → 1600 to match
   your rebalance (see open questions).

## How it was tested

Sandbox method — Chrome extension not connected (third run in a row).
- Full suite on repaired snapshots: **532 passed / 0 failed** (was 522).
- `node --check` on gameLogic, test_game, and all 12 frontend files.
- Cross-file no-undef ESLint sweep (277 shared globals via AST) across all
  12 frontend files: clean (only the known typeof-guarded
  `twemojiIsEnabled` flag).
- index.html script refs verified against frontend files; v=6/v=3 in place.
- Frontend is reload-only; backend needs a **restart** to pick up
  gameLogic.js (one-line neutral change — no rush).

Mount staleness: worst run yet. test_game.js and gameLogic.js truncated
(gameLogic cut mid-expression at 4819/4827 — would "work" until the poker
serializer); game-items/-lobby/-poker/auth truncated (lobby cut MID-LINE);
board/game-core/-init/-screen and index.html NUL-padded ("binary file
matches" in grep = padding). All snapshots repaired from host-side reads and
junction-checked before any testing.

## Heads-up: you edited while I ran

gameLogic.js and index.html changed under me mid-run (Edit collisions,
re-read and re-applied cleanly). Your edit rebalanced **bomb cost 666→1600
(classic)** and **Super Banana 777→1600**, with the frontend lobby defaults
matching. I did NOT touch your changes.

## Pending your review

- **rules.md is now out of date** on two defaults: bomb cost (says 666) and
  Super Banana price (says 777) vs 1600 in code. Per your "rules.md is source
  of truth" rule I didn't edit it — update it (or tell me to), or say if 1600
  is an experiment, otherwise the next audit run will flag these as
  violations.
- Reload check when convenient: board variations chart (board.js?v=6) and
  the menu avatar/profile name rendering (auth.js?v=3).
- ~~Still open from 22:50: dead `end_turn` socket handler in server.js~~ —
  Mark approved post-run; removed (server.js, −9 lines, syntax-checked).
  Mark also confirmed the 1600 rebalance and updated rules.md himself; the
  one stale "bombs cost more in 2v2" phrase was fixed to "cheaper".
- From 23:53: backend restart still pending for the grow-collection fix (this
  run's gameLogic change rides along).

## Open questions

1. Should rules.md be updated to bomb 1600 / Super Banana 1600, or is the
   rebalance experimental?
2. Chrome extension: three runs without a connected browser — connect it if
   you want live-render verification from scheduled runs.
