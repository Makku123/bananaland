# Session 2026-06-21 1754 — Fix tile-border "perforations" (remove always-on banana-reach)

User: "sometimes perforations appear on the border around tiles for no apparent reason. make sure to
remove this as it is a visual bug."

## Diagnosis
"Perforations" = a DASHED border on a `.space` board tile. Only two overlays drew dashed outlines on
tiles: cyan `.space.armed-path` (shown ONLY when the viewer ARMS a magic rune) and green
`.space.banana-reach`. The **banana-reach was applied UNCONDITIONALLY every render** (board.js, no
toggle) whenever the viewer owned a farm with a pile within `window._bananaGadgetN` (default 6) tiles
ahead — so it appeared with no user action = "for no apparent reason." (The Path Preview yellow
overlay is toggle-gated AND solid, not dashed; armed-path has a clear trigger.)

## Fix (frontend only)
- `board.js`: removed the `_bananaReachSet` computation block and the
  `el.classList.add("banana-reach")` line in `renderBoard`.
- `styles.css`: removed the `.space.banana-reach`, `.space.step-ahead-path.banana-reach`, and
  `.space.banana-reach.armed-path-dest` rules (the last collapsed back to just
  `.space.step-ahead-path.armed-path-dest`). Updated the overlay comment.
- KEPT (deliberate, clear trigger): cyan armed-path (on arming) + yellow Path Preview (toggle). The
  Banana Gadget's TEXT total in the panel is unaffected — only its green board overlay is gone.

## Verify
- grep: zero remaining `banana-reach` / `_bananaReachSet` references (js/css/html).
- Live: a probe `<div class="space banana-reach">` computes `outline-style: none` (perforation gone);
  `.space.armed-path` and `.space.step-ahead-path` CSS still present; the armed-dest layering rule
  `.space.step-ahead-path.armed-path-dest` survived intact. `node --check` clean; zero console errors.
- Adversarial review workflow (5 agents, 2 dimensions: "any other no-reason dashed source?" +
  "removal clean/non-breaking", each finding verified): **0 confirmed, 3 dismissed** — banana-reach was
  the only always-on dashed-tile source; no dead refs / broken selectors.

## Note
If the user also considers the cyan armed-path or yellow path-preview dashes a problem, those are
separate (both require a deliberate action) — offered to remove on request.
