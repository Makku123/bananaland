# Run summary — 2026-06-16 17:39 PT

Three gameplay/UI updates, with a multi-agent review that caught a missing
animation on the first one.

## 1. Grow fires when the Super Banana swaps onto a grow tile

- `completeSuperBananaSwap` (gameLogic.js): after the swap+reveals, if the tile
  now under the swapper (superBananaPos === their position) is a grow tile, call
  `_fireGrowAt(player, superBananaPos, "land")` so it triggers exactly as if
  landed on — grows their owned revealed farms in range, reveals, mints. Fires
  for every swap path (manual pick / random / AFK force).
- **Review-caught (major), fixed:** the backend fired correctly but the frontend
  never played the grow-chain pulse/glow for this *no-walk* landing (the pulse
  only ran in the dice-walk path + the start-pick exception). Fixed by mirroring
  the start-pick mechanism: backend sets `lastSuperBananaSwap {playerId,pos,turn}`
  (set on grow-swap, cleared each turn, serialized in getState); frontend adds a
  `_runGrowPulse` trigger keyed by it (game-screen.js), right beside the
  start-pick one. This also clears any stale pile-freeze gating, so the grown
  pile can't be hidden until the next render.

## 2. +/- banana floaters at the player's piece

- The "+N🍌"/"-N🍌" delta text now also spawns at the board **token**, in addition
  to the existing one near the banana score. Shared helpers `_spawnDeltaFloat` and
  `_playerTokenEl` (game-lobby.js); used by `_showMoneyDeduction` (loss) and
  `_bananaGainFloater` (gain). Off-board safe (no token → score floater still
  shows); works for all players; the gain guard (money actually rose) is intact.

## 3. Super Banana & Desert as plain rows (no category headers)

- Removed their standalone category sections in **both** the board-variations page
  (board.js `renderFarmTileList` → one headerless group of two `bp-tile` rows) and
  the in-game tile legend (game-screen.js `updateTileLegend` → plain `legend-row`s).
  Farms and Grow Tiles sections keep their headers.

## Verification

- **Tests:** added a backend test (grow-swap fires as a landing, an owned in-range
  farm grows, `lastSuperBananaSwap` set + serialized). **Full suite: 1118 passed,
  0 failed.**
- **Live (preview):** variations page shows only Farms + Grow Tiles headers with
  Super Banana/Desert as plain rows; the in-game legend renders 0 group headers +
  2 plain rows; gain and loss each spawn 2 delta floaters (score + token), off-board
  safe; the swap pulse trigger fires once, dedups, skips when no land-grow, and
  calls the real `_runGrowPulse` with the right args. **0 console errors.**
- **Adversarial review (3-dimension Workflow, each finding double-verified):** 1
  confirmed major (the missing swap pulse) — fixed; 0 refuted, 0 minors.

No git operations.
