# Session 2026-06-21 1427 — Starting magic runes 12 → 7

User: "give each player 7 magic runes at the start of the game."

The starting hand was **12**; changed to **7**. Single source of truth:
`gameLogic.js _freshRollCards()` — `new Array(12)` → `new Array(7)` (+ doc comment). Everything
else is length-driven (the reveal screen deals `me.rollCards`, the magic-dice panel renders
`me.rollCards`, the pstat counter shows `rollCardCount`), so no other logic changed.

## Touched (count + stale "12" text)
- `gameLogic.js`: `_freshRollCards` 12→7; 3 comments (lobby reset / seed-at-reveal / reveal-keep).
- `server.js`: 1 comment ("12 starting runes").
- `test_game.js`: assertions that pin the starting count — **19a**, **19a2**, **19g**, and **102**
  (4495/4498) → 7; plus 102's out-of-range re-roll index 12→7 (the first slot past a 7-hand) and a
  few stale comments. (All other `rollCards.length` asserts set the hand explicitly, so unaffected.)
- Frontend text: `game-items.js` comment, `game-socket.js` reveal comment, `game-tutorial.js`
  (2× "12 runes"), `index.html` help row ("12 magic runes"), `rules.md` (2×).
- Grep confirms ZERO remaining "12 …rune/dice/concealed/starting" references.

## Verify
- Suite **971 passed / 0 failed**.
- **Investigated a scare:** the suite showed 971 vs my remembered ~1053 baseline. Ran it down rather
  than trusting the number: the baseline was STALE — the user/linter added **section 107
  (Dodecahedron, a new d12 feature)** and reshaped the file between sessions. Proof: a diagnostic that
  consumed 12 randoms but RETURNED 7 (identical board layout) still gave 971 → NOT an RNG-position
  drift; and running the ORIGINAL return-12 gave 960/**11 failed**, the 11 being exactly the
  assertions I edited to expect 7. So the real total is 971, fully green with the 7-rune change, no
  regression. (Lesson logged in memory: re-run the suite to settle a count discrepancy; don't trust a
  remembered baseline when the test file is being edited live.)
- `node --check` clean on all changed JS; preview server restarts cleanly on the new backend.
- Not driven end-to-end (would need the 15s reveal); the count is unit-tested (19a/19a2/19g/102) and
  the frontend is length-driven.

## NOTE — there's now a NEW "Dodecahedron"/d12 feature (section 107) not in prior memory
`game.dodecahedron` (lobby toggle, default on), `d12OwnerId` (first SB-lander claims it), d12 owner
rolls a single 1–12 (`diceIsD12`), passed on by landing on someone. Flagged here for future sessions;
not part of this task.
