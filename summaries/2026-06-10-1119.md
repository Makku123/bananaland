# Run summary — 2026-06-10 11:19 PT (scheduled, autonomous)

Run task: pin the last unpinned money paths with tests. Audited poker betting,
tie resolution, dead-code candidates, and the ghost/Super-Banana path — all
already compliant — so this run's change is test coverage only. No production
code touched, no git operations.

## What changed

**backend/test_game.js only** (+48 asserts, 585 → 633; sections 88–89):

- **Section 88 — Poker betting money paths** (4 hands):
  1. NL min-raise rejected below previous-bet size; legal min accepted;
     re-raise below min rejected; oversized re-raise **clamped to the
     opponent's effective stack** and flagged all-in; call → runout;
     banana conservation on the full 10000 pot.
  2. **All-in shove below the legal min-raise is allowed** (standard NL);
     conservation on the 2200 pot.
  3. **Short-stacked challenger**: raise rejected (fold/all-in only), call
     for less goes all-in, **defender's uncalled 450 excess refunded**,
     auto-runout, pot trimmed to 50+50.
  4. **Tie splits the pot** (forced equal Monkey-Poker sums → both players
     end exactly where they started); also pins "defender gets their say
     after a plain call" and "defender acts first on later rounds".
- **Section 89 — Ghost on the Super Banana**: broke ghost landing on the SB
  creates the hideout-pick pending state, `forceSuperBananaSwap` (the ghost
  driver's fallback) auto-picks a genuinely hidden tile, hint recorded for
  the ghost, hideout stays hidden from everyone, swapped-in tile revealed to
  all. (The 900ms `_maybeDriveGhost` wiring itself is timer-based; pinned by
  code-read, the fallback it calls is now pinned by test.)

## Audited this run, found compliant (no changes)

- Poker engine vs rules.md betting rules: min-raise, stack cap, all-in
  refund, tie split (odd pots can't arise — matched bets keep pots even),
  short-stack opening (fold/all-in only, frontend disables raise too).
- Frontend poker UI mirrors backend caps (`minRaiseTo`/`maxRaiseTo`).
- Dead-code sweeps: all 118 gameLogic methods referenced; all 222 frontend
  functions referenced cross-file (incl. index.html handlers). Nothing to
  remove — prior cleanup runs got everything.

## How it was tested

Sandbox method — Chrome extension still not connected (seventh run in a row).
- Full suite: **633 passed / 0 failed**, stable across 6 consecutive runs
  (new tests include randomness; conservation asserts are random-proof).
- **Negative checks**: three scratch mutations of gameLogic.js (drop
  min-raise rule, drop all-in refund, award tie to defender) each fail the
  new asserts; gameLogic.js restored byte-identical (md5 verified).
- Host-vs-sandbox fingerprints verified before and after (`section(` 95/95,
  distinctive new strings 4/4).
- Mount note: virtiofs stale cache struck again — sandbox served truncated
  gameLogic.js (cut mid-line at 4815/4827 lines), test_game.js (3685/3929),
  game-items.js, game-screen.js, and NUL-padded server.js + game-socket.js.
  All work ran on host-spliced copies in /tmp; the HOST files (edited via
  host tools only) are ground truth and were fingerprint-matched against
  what actually ran.

## Pending your review

- **Restart the backend** — still pending since the 06-09 runs (earlier
  runs' gameLogic/server changes aren't live until then). This run adds no
  new restart requirement (tests only).
- No sign-off needed for this run's change: test-only, all green.

## Open questions

Carried from 04:19 (still unanswered, no action taken): should the
item-auction teammate 5s wait also apply during the silent tie-breaker
phase? rules.md only specifies the wait for *Accept*, so the implementation
does exactly that.
