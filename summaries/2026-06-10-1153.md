# Run summary — 2026-06-10 11:53 PT (scheduled, autonomous)

Run task: check compliance with today's rules.md edit, then remove the dead
`growSquatterSteals` plumbing. Also fixed a flaky assert in last run's
section 89. No git operations.

## rules.md change audit (edited ~11:31 today)

The newest clause — "You cannot vine swing to an allied farm in 2v2 mode" —
is **already compliant**: backend `_validateTeleportTarget` requires
`owner === player.id`, the frontend picker mirrors it, and
test_game.js already pins "Vine Swing to a TEAMMATE's farm rejected"
(now at line 3633). No code change needed. (If you added this line after
seeing an allied swing happen in a live game, that's the stale server — the
fix has been in the host files since the backend-restart note below.)

## What changed

**Dead-code removal — `growSquatterSteals`** (vestige of a removed
"squatter steals on grow" mechanic; the backend only ever nulled and
serialized it, never set it):

- `backend/gameLogic.js` (−6 lines): five per-turn null resets and the
  `getGameState` serialization entry.
- `frontend/board.js` (−42 lines): the unreachable grow-then-steal floater
  animation block (`_applyStealFloaterStyle` / `_isFriendlySteal` are still
  used by live steal paths and stay).
- `frontend/game-screen.js` (−9 lines): the `_squatterStealIds` skip set in
  the deferred-effects loop (always empty).
- `backend/test_game.js`: deleted dead section 57 ("Squatter Steal on GROW" —
  its asserts sat behind a never-true conditional and asserted the *removed*
  behavior). The current rule (pile grows under a squatter, steal only on
  leave) is already pinned by sections 65, 65e, 65f. Also dropped the
  vacuous `!game.growSquatterSteals` assert in section 65.

**Flaky test fix — section 89** (added last run): its
`game.properties.get(sbPos).group` assert crashed whenever
`forceSuperBananaSwap` randomly picked a hidden *grow* tile as the hideout
(grow tiles have no `properties` entry — legal per rules.md "pick any hidden
tile"). Last run's "stable across 6 runs" was luck; it failed 1 in ~6 here.
Now tolerates a non-property swapped-in tile. Suite count: 633 → 632
(the dropped vacuous assert; section 57's conditional asserts never counted).

## How it was tested

Sandbox method — Chrome extension still not connected (eighth run in a row).
- **Full suite: 632 passed / 0 failed**, 13 consecutive green runs
  (also exercised the new flaky-fix path repeatedly).
- **Negative check**: scratch mutation of gameLogic.js (squatter pockets
  growth instantly) fails 4 asserts in sections 65/65-adjacent — the rule is
  still guarded after the cleanup. gameLogic.js restored md5-identical.
- **Frontend cross-file no-undef lint**: eslint 10 with all top-level
  declarations across the 12 frontend files + index.html inline scripts as
  shared globals — **0 errors** after the removals.
- Mount note: virtiofs stale-attr cache again served truncated/NUL-padded
  copies (test_game.js cut at 3668/4040 lines, game-items.js at 989/1031,
  NUL tails on server.js, board.js, game-screen.js, game-socket.js).
  Direct-I/O reads gave current content within the stale length; tails were
  spliced from host reads. All tests ran on the spliced /tmp copies;
  host files (edited via host tools only) fingerprint-matched what ran
  (section counts, distinctive strings, zero `growSquatterSteals` repo-wide).

## Pending your review

- **Restart the backend** — still pending since the 06-09 runs. Today's
  gameLogic.js change is removal-only, but the restart also picks up the
  earlier rules fixes (including the vine-swing teammate rejection).
- No sign-off needed for this run: dead-code removal + test fixes, all green.

## Open questions

1. (Carried from 06-10 04:19, still unanswered) Should the item-auction
   teammate 5s wait also apply during the silent tie-breaker phase? rules.md
   only specifies the wait for *Accept*, so the implementation does exactly
   that.
2. (New) rules.md says a Super Banana hideout can be "any hidden tile" —
   including hidden grow tiles, which is what made section 89 flaky. Is a
   grow tile as a hideout intended? If you'd rather restrict hideouts to
   farm tiles, that's a small backend change + rules.md edit.
