# Session summary — 2026-06-09 18:13 PT (scheduled run)

## Task

Dead-code removal: the legacy Vine Swing TILE machinery, `bus`/`tax10`
tile handlers, and the `upgrade_magic_dice` no-op (flagged as a cleanup
candidate by the 17:34 run). The board has had no bus/tax tiles since it
became 40 farms + 6 grows + desert + Super Banana, and Vine Swing became
an item (`_useTeleportCard`), so `this.vineSwing` could never be set —
everything keyed on it was unreachable.

## What changed

**backend/gameLogic.js**

- Removed the `space.type === "bus"` / `"tax10"` branches from
  `_processLanding` and `_processLandingPassive`, and the tax10 branch in
  the Super-Banana swap-in path (plus the now-unused `newSpace` lookup).
- Removed `vineSwingMove()` and every `this.vineSwing` init/guard/clear
  (notifyAnimsComplete, ghost driver, rollDice/useMagicDice/useCard/
  debug-move auto-end checks, pickStartTile, poker dismiss paths,
  removePlayer, reset) and the `vineSwing` field from `getState`.
- Removed the `upgradeMagicDice()` no-op.

**backend/server.js** — removed the `vine_swing_move` and
`upgrade_magic_dice` socket handlers.

**frontend** (game.js, board.js, index.html, styles.css)

- Removed: vine-pick click emit + `space-pickable` highlighting (three
  sites), the `gs.vineSwing` transition detectors, `vine-activate` tile
  animation + keyframes, `_vineSwingJustLanded` flag plumbing,
  steal-suppression checks keyed on it, the "Auto Vine" debug checkbox
  and its auto-pick block, the `upgradeMagicDice()` emitter, the
  `.type-tax10` / `.type-bus` CSS rules, and the legend's Vine Swing /
  "-10% Peel" rows (counted types that can't exist).
- **Kept-alive bonus:** `playVineSwing()` (the swing swoosh) was only
  wired to the dead tile path, so the item teleport was silent. It now
  fires when a new `lastTeleport` arrives (deduped by turn + player).

**Tests (backend/test_game.js)** — deleted dead sections 20 (poked
`game.vineSwing` directly), 39 (Tax Tile), 52/53 (bus-tile Vine Swing)
and the upgradeMagicDice no-op asserts; board-composition test now
asserts `other === 0` (no legacy tile types) instead of counting
bus/tax10; state pokes no longer reference `game.vineSwing`. The live
Vine Swing ITEM tests (section 68 etc.) are untouched and still pass.

**TECHNICAL.md** — dropped the two removed socket events.

## How it was tested

Game not running on the host → sandbox:

- Full suite ×6: **506 passed / 0 failed** every run (513 baseline; the
  7 missing asserts are the deleted dead-code tests themselves).
- `node --check` on all five touched JS files.
- Server boot (JWT_SECRET set, Linux rebuild of better-sqlite3) + HTTP
  smoke: index/game.js/styles.css all 200; served index has zero
  `chk-auto-vine` refs, served game.js zero `vineSwing` refs.
- Mount quirk handling: the sandbox mount NUL-pads shrunk files, so all
  checks ran on NUL-stripped copies; every touched file shrank (no
  truncation risk). Commit built via git plumbing from stripped copies.

## Pending / open questions

- **Desert rules mismatch (needs Mark's ruling):** rules.md says the
  desert "does nothing; a '0 farm' you just pass over", but the code
  deliberately makes it BUYABLE — landing on it opens a banana-bid
  auction like any farm (explicit design comment in gameLogic.js, and a
  desert-auction test exists). Left as-is: recent history (the 16:33
  grow change Mark reversed) says don't take rules.md literally against
  deliberate code. Which is right — auctionable cactus or pure
  pass-over? rules.md or code should be updated to match.
- styles.css still has rules for other nonexistent tile types
  (`type-chest`, `type-auction`, `type-birthday`, etc.) — same cleanup
  category, left out to keep this change reviewable.
- Still open from earlier runs: 10s grace window on the ghost-concede
  win (16:57) awaits confirmation; debug_* socket events aren't
  environment-gated.
- Push from the sandbox may still fail (no GitHub credentials in prior
  runs); if so develop will be ahead of origin and Mark should push.
