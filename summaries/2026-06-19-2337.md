# Session 2026-06-19 2337 — Roll-card UI: vertical cards, 6 revealed, profile counters on own line

Three requested updates to the guaranteed-roll UI.

## 1. Guaranteed-roll chips → vertical rectangles (card-shaped)
`styles.css` `.roll-chip`: 50×50 square → **46×64 vertical rectangle** (border-radius 9px, font
1.6em). The ×N pill (`.roll-chip-mult`), revealed tag (`.roll-chip-tag` + `.has-tag` val-shift),
armed ring (`.roll-chip-armed`), disabled/hover/focus states all still position correctly on the
taller card.

## 2. Start with 6 revealed + 6 unrevealed (12 total)
`gameLogic.js` `_freshRevealedRollCards()` now returns **6** (was 3); concealed stays 6. Updated
the completeReveal/reset comments. Tests `19g0`/`19g0b` now assert 6 revealed at start/reseed.

## 3. Profile box → counters on their own line + more padding
`styles.css`:
- `.players-box .pstat` now `flex-wrap: wrap` with padding `6px 10px` → **`8px 11px`**.
- `.pstat-cards` (the mini-card counters) moved from inline-after-money to its **own full-width line
  below** the name/score: `flex: 0 0 100%` + a subtle top divider. So a full hand never crowds the
  name. `.pstat-me` padding bumped to match.

## Verify (live, real 2-player game)
- The Guaranteed Rolls panel renders chips as **vertical cards** (green concealed w/ ×N pills + armed
  ring; gold revealed w/ tags).
- Real game state: P1 has **6 concealed + 6 revealed**.
- MONKEYS panel: each player's card counters sit on **their own line below** name+score, with the
  divider + padding; they fit.
- No console errors. Backend **1168 passed, 0 failed**; pile harnesses 43/43 + 20/20.
- Adversarial review (2 surfaces): **CSS clean — no regressions** (`.roll-chip`/`.pstat`/`.pstat-cards`
  scoped to the game screen only; money still right-aligns on line 1; board.js/game-init.js DOM
  queries target first-line elements, unaffected). Count change: implementation + tests correct; it
  flagged stale **comments/docs** still saying "3" — **all fixed** (gameLogic.js reset + addPlayer
  comments, 3 test comments, and a contradictory rules.md line that claimed "no revealed cards" at
  start → now "6 revealed").

## Docs / memory
- `rules.md`: starting hand 6+6 (12 total); the "Revealed cards" bullet now says you start with 6.
- Memory updated (revealedRollCards starts at 6; 12 total).
