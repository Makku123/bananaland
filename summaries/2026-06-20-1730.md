# Session 2026-06-20 1730 — Desert tile listed in the Farm section everywhere

Per the user: "put the desert tile in the farm section … in game, in board variations,
and the shuffling screen." Pure **frontend grouping** change — the backend desert is
untouched (still `group:"desert"`, price 0, auctions like a farm).

The desert was its own category in three render paths; folded each into Farms:

1. **In game** (`frontend/game-screen.js`):
   - `updatePropertyChart` maps `group:"desert"` into the **"farm"** chart group (an
     `isDesert` flag → renders "🌵 Desert / grows nothing", `.chart-item--desert`,
     sorts first at price 0).
   - `updateTileLegend` dropped the standalone Desert entry → legend = Super Banana only.
2. **Board variations** (`frontend/board.js renderFarmTileList`): the desert is appended
   to the **Farms grid** as a `.bp-yield-chip--desert` 🌵 chip (filter
   `t.group==="desert" || t.type==="desert"`); farm header meta notes "+ 🌵 desert" and
   yields start at 0; removed the desert row from the misc section.
3. **Shuffling/reveal screen** (`frontend/game-socket.js _renderRevealContent`): the
   desert tile(s) append into the **Farms reveal-group** as `.reveal-tile--desert`; the
   farms section fires if farms OR deserts exist (lo=0 when a desert is present); removed
   the standalone Desert reveal-group.

CSS: `.bp-yield-chip--desert` (sandy chip) + `.chart-item--desert` (tan tint) added.

## Verify
- Frontend harnesses 43 + 20; backend untouched (suite stays 1012/0).
- **Live (DOM, screenshots still wedged):** (1) `renderFarmTileList(buildPreviewLayout())`
  → desert chip in `.bp-group--farms` grid, none in misc. (2) `_renderRevealContent` with a
  stubbed boardLayout → 3 groups (Farms / Grow / Super Banana), desert inside the Farms
  group, header "yields 0–N", no standalone Desert header. (3) `updatePropertyChart` with a
  stubbed boardLayout → Farm Chart has `.chart-item--desert` "🌵 Desert grows nothing", Tile
  Legend = "⭐ Super Banana" only (no Desert). Zero console errors.

## Note
No fog-of-war change: the Farm Chart still only lists revealed tiles and shows no tile
positions (pos is used only for owner dot / dice glow), so the desert-as-farm leaks nothing
its farm neighbours didn't already. The pre-reveal "a desert exists" legend hint is gone by
design (farms aren't pre-announced either).
