# Run summary — 2026-06-18 19:14 PT

Self-directed bug-hunt run focused on **special items + movement** (TODO file was
emptied this run, so no TODO items were used). Verified Vine Swing end-to-end and
fixed a real **UI/help-text inconsistency**.

## Finding & fix — Vine Swing help text said "any tile"
The in-game **Help panel** described Vine Swing as "teleport to **any tile**" in
two places, which contradicts both the implementation and rules.md. The actual
ability only swings to **a farm you own** (and, in 2v2, never a teammate's farm).

- **`frontend/index.html`** — both Help-panel rows changed from
  "teleport to any tile" → "teleport to **any farm you own**" (the won-item legend
  row and the Special-items row).
- **`backend/gameLogic.js`** — stale comment above `_useTeleportCard` ("jump to
  ANY tile without walking") corrected to "swing (no walking) to a farm YOU own"
  so the comment matches the function it documents.
- `game-tutorial.js` already said "any farm you own" — no change needed.
- No code-behaviour change: `_validateTeleportTarget` already enforces
  own-farm-only, matching rules.md.

## Vine Swing behaviour — adversarially verified (throwaway harnesses, deleted)
Drove the public API (`useCard` / `_validateTeleportTarget`) after advancing past
the reveal phase (`completeReveal()`). **16/16 FFA checks + 3/3 team checks
passed, 0 issues:**
- Allowed: swing to a farm you own (moves, consumes the card, fires landing).
- Rejected (and card retained): opponent's farm, unowned farm, the tile you're
  already on, out-of-range / negative / non-integer positions, after the dice are
  rolled, without holding the item, off-turn, and a second card in one turn.
- **Team (2v2):** swinging to a **teammate's** farm is rejected and keeps the
  card — matches rules.md ("You cannot vine swing to an allied farm in 2v2").
- Note: items are intentionally unusable on a player's start-pick (first) turn —
  not a bug.

## Verification
- **Full suite: 1187 passed, 0 failed**, deterministic ×2.
- `node --check` clean on `gameLogic.js` and **every** `frontend/*.js`.
- Edited Help rows confirmed to read "any farm you own".
- All throwaway harnesses ran under `/tmp` and were deleted; repo grep shows no
  residue.

## Process note
Per Mark: rules.md is the **intended design**, but it is **not an infallible
source of truth** — it can contain internal inconsistencies. When code and
rules.md disagree, that's still worth fixing toward rules.md *if* rules.md is
self-consistent; if rules.md itself looks inconsistent, flag it to Mark with a
multiple-choice question instead of silently conforming. This run's discrepancy
was UI-vs-(code+rules.md), with rules.md self-consistent, so the fix was
unambiguous.

No git operations.
