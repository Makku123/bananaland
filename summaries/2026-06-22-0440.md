# Session 2026-06-22 0440 — Move SB cost off the tile → Tile Legend (target + cross bonus)

User: "remove the super banana cost from the super banana tile and just show somewhere else what the
'target bananas' is. also show in the same area what the cross bonus is."

## Change (frontend only)
- **`board.js` renderBoard SB branch** (`tile.group === "superBanana"`): removed the
  `<span class="sprice">${tile.price}🍌</span>` — the in-game Super Banana tile no longer shows its
  cost. The rainbow-banana SVG + the `.sb-cross-hint` ("+200 ←") stay.
- **`game-screen.js` `updateTileLegend`**: under the existing "⭐ Super Banana — win the game" row,
  appended a `.legend-sb-stats` block with two rows:
  - `🎯 Target bananas` → `gs.superBananaPrice`🍌 (the configured win price = your target; fog-independent).
  - `↩️ Cross bonus` → `+200`🍌 (green; flat, matches `_resolveSuperBananaCross` broke-cross + the on-tile hint).
- **`styles.css`**: `.legend-sb-stats` / `.legend-sb-stat` / `.legend-sb-label` / `.legend-sb-val`
  (+ `--bonus` green) — a subtle gold-tinted box in the Tile Legend panel.

The "area" = the **Tile Legend** panel (right under the board's Grow Chart), which already had the
Super Banana entry — so target + cross bonus sit with it.

## Verify (screenshots wedged → DOM + computed-style)
- Stubbed `gs` and called `updateTileLegend`: legend renders "⭐ Super Banana — win the game", then
  "🎯 Target bananas 10000🍌" and "↩️ Cross bonus +200🍌" (bonus computed color rgb(109,255,160)).
- `grep` confirms the renderBoard SB branch no longer emits `.sprice` (remaining `.sprice` hits are
  regular farms + the separate board-variations *preview* showcase).
- `node --check` clean (board.js + game-screen.js); zero console errors.

## Notes
- Cross bonus is a hardcoded **200** (backend `_resolveSuperBananaCross` + the board `.sb-cross-hint`
  + now the legend). Not a setting — if it ever becomes configurable, ship it in getState and read it
  in all three spots.
- Scope: only the **in-game** SB tile lost its cost. The **board-variations preview** (board.js
  ~1490/1495, a lobby showcase) still shows it — left as-is since it's a different view; easy to remove
  there too if wanted.
- Left the legend row's "×1" count (removing it meant editing an escaped-char block; harmless).
