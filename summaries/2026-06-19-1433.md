# Session 2026-06-19 1433 — Remove stale board-help mechanics (chains, fixed-GROW% corners, send-bananas) + dead code

The in-game board help (`frontend/index.html` `.board-help-body`) described three mechanics that no longer exist. Investigated each (parallel workflow + independent verification) to confirm DEAD vs LIVE before deleting, then cleaned up the help text AND the orphaned code.

## Verdicts (all confirmed DEAD, with the LIVE look-alikes preserved)
- **Chains (adjacent same-color farms → 2×/3×/4× yield):** DEAD. `_fireGrowAt` growth is a flat `amount = prop.price` (no multiplier); board.js `_chainMultipliers` is declared but never populated, so `chainMult` is always 1 (no-op). LIVE look-alike kept: the grow-CHAIN light-sweep animation/range (`_growRange`, `_growRangePath`, `playGrowChainStop`, the grow-chain bounce/`.grow-fired`).
- **Fixed grow tiles / "Corners (fixed GROW%)" (100/25/50/75):** DEAD. The board is explicitly **cornerless** (`BOARD_SIZE=48`, GROW_POSITIONS only); the only `growPct` anywhere is `1.0`; growth has no `×GROW%` or `×Chain` factor. LIVE look-alike kept: the GROW (1–6) labeled tiles (`_fireGrowAt`/`_processRolledGrow`, `growPct=1.0`) and the cosmetic "corner" layout label in board.js/CSS.
- **Send bananas to teammate (2v2):** DEAD. No `send_bananas`/`transfer_bananas`/`tradeBananas` socket handler or gameLogic method or frontend UI exists (a prior summary, `2026-06-17-0053.md`, records removing `tradeBananas` + the `trade_bananas` handler). LIVE look-alikes kept: `transfer_host` (lobby host handoff) and the 2v2/3v3 farm-auction teammate flow.

## Cleanup
- **frontend/index.html (board help):** "Farms & Chains" → "Farms" (dropped the chain row + the fictional "Growth = Yield × GROW% × Chain" formula row); removed the entire "Corners (fixed)" section; removed the entire "Trading" section. Kept the live rows (roll-match 100% grow, Squatter, GROW (1–6), etc.).
- **frontend/board.js:** removed `const _chainMultipliers = {}` and the no-op `chainMult`/`effectiveYield` multiply (now `priceDisplay = \`${tile.price}🍌\``).
- **frontend/styles.css (4 orphaned blocks, zero references anywhere):** `.tut-chain-*` (chain-multiplier tutorial demo), `.help-corners-grid/.help-corner*` (corners help grid), `.trade-panel*` and `.trade-section/.sell-*/.send-banana-numpad` (the removed trade/send-bananas panel). Removed via an anchored Node splice (ASCII anchors, utf8/CRLF-safe).

## Verified
- `node --check board.js` clean; CSS brace-balanced (1833/1833); all files pure CRLF.
- Repo-wide grep: ZERO remaining references to any removed selector/identifier; the live boundary rules (`.space.g-pink`, Auth & Profile, Tutorial nav, Help Tile List) intact and adjacent.
- Live DOM (preview): help sections now **Farms · Special Tiles · Auctions · Monkey Poker · Guaranteed Rolls**; no chain/corner/trading/send-to-teammate text; GROW (1–6) + Squatter preserved.
- No backend logic changed; backend test suite unaffected (1115 still green from the prior session).
