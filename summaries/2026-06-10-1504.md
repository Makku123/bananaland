# Run summary — 2026-06-10 15:04 PT (scheduled, autonomous)

Run task: add the **banana-conservation invariant** to the test fuzz (the
extension suggested in the 12:27 run), backed by a small mint/burn ledger in
gameLogic. This puts a net under the whole "money leak / money dupe" bug
class — any subsystem that creates or destroys bananas off the books now
fails the fuzz within a few steps, with the exact ledger delta in the
message. No git operations; rules.md untouched.

Before settling on this, audited three not-yet-covered areas against
rules.md — **poker betting** (min-raise, all-in trim, smaller-stack cap,
short-stacked challenger), **2v2 trades** (fee, half-stash cap, teammate
gate), and **Vine Swing / item arming / start-of-game item grant** — and
found all of them compliant; no rules violations to fix this run, hence the
pre-approved test-infrastructure task.

## What changed

**`backend/gameLogic.js`** (+~35 lines, all inert bookkeeping):
`this.bananaLedger = { baseline, minted, burned }` is created at game start
(completeReveal) and nulled in the lobby reset. Every money mutation site
now records its flow:

- **Mints:** grow growth (per-farm yield in `_fireGrowAt`), the debug
  +10000, and the defensive negative-money clamp in `endTurn` (mints the
  forgiven shortfall, so a bug that drives money negative still shows up as
  a ledger mismatch BEFORE the clamp papers over it).
- **Burns:** farm-auction payment, item-auction payment (the *actual*
  deducted amount — the existing clamp forgives shortfalls), Super Banana
  purchase, bomb purchase, the 2v2 trade fee, a leaver's vanished
  money/piles in `removePlayer`, the no-live-placer estate in
  `_bombEliminate`, and pot-with-no-recipient edges in poker resolution.
- **Transfers** (skims, steals, collections, poker pots, bomb loot, trade
  principal) intentionally don't touch the ledger.

`bananaLedger` is not serialized in getState — it's internal + test surface
only. Production behavior is unchanged (verified by the suite: every
pre-existing assert still passes).

**`backend/test_game.js`** (718 → 743 asserts):

- **Fuzz upgrade (section 90):** `_fuzzInvariants` now checks, after every
  driver step of every game (incl. the ghost games):
  `sum(money) + sum(piles) + live poker pot == baseline + minted - burned`.
- **New section 94** (6 blocks): pins each mint/burn site deterministically
  so a regression names the site instead of a generic fuzz leak — grow
  mint + steal-is-transfer, auction burn (exact price), poker pot as
  escrow + payout balance, 2v2 trade fee (exactly 50 on a 1000-start),
  bomb purchase + elimination transfer + defensive-path burn, Super Banana
  purchase burn.

## How it was tested

Sandbox method — Chrome extension not connected (checked again this run).

- **Full suite: 743 passed / 0 failed, 10 consecutive identical runs.**
- **Negative checks** (scratch mutations of the test copy only):
  1. Drop the trade-fee burn → the fuzz catches it organically on BOTH 2v2
     seeds ("banana leak: 16880 != 17130 …") and 94d pins it — 4 failures,
     nothing else.
  2. Drop the grow mint → leaks surface on 5 of 6 fuzz seeds plus the ghost
     games and section 94a — 14 failures, all leak-related.
- **Server smoke:** booted server.js on :3212 with the new gameLogic
  (JWT_SECRET stub) — `/` 200, socket.io polling 200.
- `node --check` green on the assembled gameLogic.js and test_game.js.
- Frontend untouched — no lint sweep needed.
- Mount note: virtiofs attr cache stale again (gameLogic cut mid-token at
  line 4800, test_game at 3668). New trick that saved ~1400 lines of
  context-shuttling: **the 13:59 run's green /tmp scratch copies survive in
  the VM** (`/tmp/bl2_25566`), and `diff` proved its prefix byte-identical
  to the mount's current view — so the test copy was assembled as
  base + the three known insertions (section 93 from host reads, my two
  edits), then fingerprint-matched against the host line-for-line (every
  marker at the same line number: 4124/4686/4768/4891). Host files were
  edited with host tools only.

## Pending your review

- **Restart the backend** (carried since 06-09): the auction-stall fix,
  ghost-skim fix, bomb-lifecycle fix, and now the ledger are all waiting on
  a restart to go live.
- Design note: the ledger treats the endTurn negative-clamp as a MINT (so
  conservation holds and the underlying bug is still visible as a pre-clamp
  dip). If you'd rather the clamp be an invariant violation outright, say
  so and I'll assert `money >= 0` in the fuzz instead.

## Open questions — ALL CLOSED by Mark's poll (~15:10 PT, same day)

1. Bomb countdown: **placer's turns — keep code.** rules.md reworded to
   "3 of the placer's own turns" (edited 15:10).
2. Teammate 5s wait: **Accept only — keep.** No change.
3. SB hideout on a hidden grow tile: **intended — keep.** No change.
4. server.js dead surface: **keep for now** — do not remove without a fresh
   go-ahead.

Nothing carried forward except the backend restart.
