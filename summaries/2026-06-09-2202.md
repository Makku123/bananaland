# Session summary — 2026-06-09 22:02 PT (automated hourly run)

Task picked: the pending item from the 21:51 run — the stale "Auctions"
tutorial slide in game.js. On inspection most tutorial slides contradicted
rules.md, so this run brought the whole tutorial into compliance.

## What changed (frontend/game.js, TUTORIAL_STEPS only)

- **Auctions (slide 5, the flagged one):** full rewrite. It described
  bidding on *owned* farms with owner accept/reject — a mechanic that
  doesn't exist. Now describes the real flow: land on an unowned farm →
  Pitch → Respond (secret accept/reject) → Silent tie-breaker (tie keeps
  it with the lander), plus broke-lander sealed bid and Buy Now.
- **Dice & Movement (2):** "each die face fires its GROW" → dice **sum**
  fires a revealed GROW before moving (7+ matches nothing); "collect piles
  on farms you cross — even ones you don't own" → only your **own** farms
  collect on cross/land.
- **Banana Economy (4):** GROW described as sweeping clockwise over
  *revealed* farms you own up to the next revealed GROW (was "every farm
  you own"); squatting steals on leave, owner can deny.
- **Monkey Poker (6):** trigger is landing where **exactly one opponent**
  stands (was fictional "poker challenge tiles"); defender antes 10%,
  challenger folds free; default variant 3 cards over 3 rounds (was
  "cards numbered 1-10"); Hold'em noted as lobby option. Card demo now
  shows 3v3 cards.
- **Special Items (7):** item auction trigger is the shared counter
  (default 50) hitting 0, starter sees the item, others bid blind (was
  "every few rolls a random auction"); Vine Swing → "any farm you own"
  (was "any tile of your choice").
- **Pineapple Bombs (8):** corner-to-corner blast both directions (was
  "entire 12-tile side"); hidden until placer's next turn ends, then
  3-turn countdown or instant on landing (was "expires after 8 turns" —
  bombs don't expire); no corner placement, one per tile.
- **Game Modes (9):** 2v2 transfer fee corrected to 5% of starting stash
  (111🍌 default, was "50🍌"); added the bomb-elimination win condition.

No backend changes. No git operations.

## How it was tested

Game not running on host → sandbox (mount corruption again required
fingerprint-checking every copy against host file tools):

- Host file verified directly (Grep): all stale phrases 0 hits, new
  phrases present; net +1 line (6,959).
- `node --check` on cleaned game.js: pass.
- All 10 TUTORIAL_STEPS `render()` executed in Node: all return valid
  HTML strings.
- Backend suite on a verified-fresh copy (test_game.js tail re-spliced
  from host reads — same truncation as prior runs): **504 passed /
  0 failed**, matching the 21:51 baseline.
- Full boot: fresh backend + corrected frontend, HTTP smoke `/`,
  `/index.html`, `/game.js`, `/board.js`, `/styles.css` all 200; served
  game.js is the corrected 6,959-line file (5 new-phrase hits, 0 stale).

## Mount corruption observed this run

- game.js reads through the mount: NUL padding once, and one read with
  ~890 duplicated lines (7,849 vs 6,959) that contained both old AND new
  tutorial text — dangerous for verification if unchecked.
- backend reads: gameLogic.js came through stale+oversized on one copy
  (5,819 lines incl. removed sell code); test_game.js tail truncated
  mid-line at 3,374/3,391 on every mount read.
- Stale root-owned files in /tmp (`/tmp/bl`, `/tmp/srv.log`) from earlier
  runs block reuse; worked around with mktemp dirs.

## Pending / carried forward

- 10s ghost-concede grace window still awaits Mark's explicit
  confirmation (behavior unchanged; tests cover it as-is).
- Mount corruption keeps recurring and is getting riskier (a corrupted
  read silently contained pre-edit text). Worth investigating outside
  the hourly runs.
- Mark should hard-refresh the browser (game.js is client-side) to see
  the updated tutorial.
