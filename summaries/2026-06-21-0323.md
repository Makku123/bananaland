# Session 2026-06-21 0323 — Remove the magic-rune NEGATIVE mode entirely

User: "remove the negative mode toggle from the magic runes." Asked scope → user chose
**"Remove the whole feature"** (not just the button).

Negative mode = a per-player toggle that made a played rune of value N move **13 − N** (1→12, 6→7).
Fully removed across backend + frontend; played runes now ALWAYS move their face value N.

## Backend
- `gameLogic.js`: removed player field `negativeRolls` (init + both reset sites); `useRollCard`
  movement simplified to `const move = N` (was `cur.negativeRolls ? 13 - N : N`); removed the
  `setNegativeRolls(socketId,on)` method; removed `negativeRolls` from `getState`. Also fixed a stale
  comment in `_processRolledGrow` ("Magic Runes step > 6" → "2d6 dice SUM of 7..12"; the `>6` guard
  stays — it's for dice sums, which still reach 7..12).
- `server.js`: removed the `set_negative_rolls` socket handler.

## Frontend
- `game-items.js`: removed `setNegativeRolls` + `toggleNegativeRolls`.
- `index.html`: removed `#btn-negative-rolls` and the `.die-numeral` overlay (it only existed to show
  7..12 for negative plays; runes/dice faces are 1..6).
- `game-screen.js`: SB win-hint simplified (no `requiredNegative`/`modeNote`; `needed` must be 1..6);
  `setDieFace` now only handles 1..6 (dropped the `>=7` numeral branch); `resetDieOverlay` no longer
  touches a numeral; rune-chip rendering dropped all negative branches (`const negative`, toggle-button
  update, `displayVal`/13−v, `concealedGroups.reverse()`, `negative-mode` class).
- `board.js`: armed-path preview `steps = v` (was `negativeRolls ? 13 - v : v`).
- `styles.css`: removed `.btn-negative-rolls*`, `.magic-dice.negative-mode #magic-dice-list`,
  `.die-numeral`, `.win-hint-neg`.

## Verify
- Case-insensitive grep across js/html/css: ZERO leftover references to the feature (all remaining
  "negative" hits are unrelated — negative index/bid/amount, negative-money clamp, floater comments).
- Backend suite **1053 passed / 0 failed**, run 3× consecutively (exit 0) — deterministic & green.
- `node --check` clean on all changed JS.
- Live DOM (preview): the toggle button + `.die-numeral` are gone; the die cube + its 6 faces remain
  intact; magic-dice panel/list intact; `setNegativeRolls`/`toggleNegativeRolls` undefined; no HTML
  reference to the handler; zero console errors.

## Review (ultracode)
5-agent adversarial review (leftovers / backend / frontend, each finding verified). Result: **1
confirmed = the stale `_processRolledGrow` comment** (nit, fixed above). The other reviewer claim — "
suite is 1045/6 failed" — was NOT reproducible: 3 consecutive direct runs give 1053/0. The suite count
drifted during the session (1051→1053) because the user/linter was editing files, so that agent almost
certainly ran the suite mid-write; the current tree is green. No defect from the removal.
