# Session 2026-06-19 1842 — Cancel Guaranteed Roll: 2 charges each (both modes)

Request: give all players **2 Cancel Guaranteed Roll charges each**, in team mode AND free-for-all.

## Change
The cancel ability already gave ONE charge in both modes (no per-mode branch). Bumped the
starting charges and the refund cap from 1 → 2.

- `gameLogic.js`: `cancelItems = 2` at all three init sites (`addPlayer`, `completeReveal`,
  `_resetToLobby`); `_cancelChargeMax()` `1 → 2` (the refund cap). The three refund sites
  (HIT in `_resolveCancelOnRoll`, target-ghost in `makeGhost`, target-leave in `removePlayer`)
  already use `Math.min(x+1, _cancelChargeMax())`, so they now cap at 2 automatically. Updated
  the `addPlayer` + `useCancelItems` docstrings (the latter was caught by the verify workflow as
  still saying "one charge / only one cancel at a time").
- Net behavior: you can hold up to **two** cancels out at once, but only on **different** targets
  (the "no second cancel on the same target" guard is unchanged).
- `server.js`: no change (handlers just delegate).
- Frontend: the cancel button text `🚫 Cancel Guaranteed Roll (N left)` is purely
  `me.cancelItems`-driven (game-screen.js ~906) — no logic change. Fixed a stale (and previously
  already-wrong) comment in `game-items.js` that claimed "Free-for-all: 2 charges. 2v2: 1 charge".
- `rules.md`: "two Cancel charges … up to two at a time on different players".

## Tests
Reworked every cancel-count assertion in `test_game.js` for start=2/cap=2:
- start "TWO charges"; cast spends 2→1; a **second** cast on a DIFFERENT player now allowed (1→0);
  a **third** blocked (exhaustion test grown to 4 players for a 3rd target); refund cap asserts ≤2;
  HIT/leave/ghost refunds go "back to 2"; owner getState sees their count, opponents see `undefined`
  (JSON-dropped). Cleaned 19g4 + 96c to use the natural start=2 flow instead of manual `=1` sets.
- **1133 passed, 0 failed** (was 1130). Frontend harnesses unaffected: pile-bounce 43/43,
  pile-persist 20/20.

## Verify
- Standalone node check: start=2 + cap=2 in classic / 2v2 / 3v3; a cast-then-HIT refund correctly
  caps at 2 (not 3); getState ships `cancelItems` owner-only.
- Browser (preview, fresh server): cancel button renders **"🚫 Cancel Guaranteed Roll (2 left)"**,
  enabled; no console errors.
- **Adversarial verification workflow** (5 agents, 4 surfaces + completeness critic): confirmed the
  1→2 change complete/correct everywhere. It flagged 2 high-sev STALE COMMENTS (gameLogic.js
  `useCancelItems` docstring + game-items.js) and 2 low-sev test cleanups (19g4/96c manual sets) —
  ALL fixed. Final grep sweep: no remaining "one charge"/"single charge"/"only one cancel"/"2v2: 1
  charge" wording in production code or docs.

## Notes
- The refund cap MUST move with the starting count: leaving it at 1 would let a player start with 2
  but never refund above 1 (degrading the second charge after the first cast). Both are now 2.
- `_cancelChargeMax()` remains the single source of truth for the cap (all refunds route through it),
  so a future tweak is one line.
