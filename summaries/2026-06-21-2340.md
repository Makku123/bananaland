# Session 2026-06-21 2340 — Remove the pre-game rune re-roll (mulligan)

User: "make it so that players cannot reroll their runes at the start of the game."

Removed the PRE-GAME mulligan (re-rolling starting runes during the "revealing" phase). The reveal
phase still SHOWS the 7 dealt runes (deal-in animation); they're just display-only now. **Untouched:**
the separate in-game REDRAW (Super Banana / cancel reward: draw a rune, keep-or-reroll at turn start —
`this.redraw`/`rerollsLeft`/`redrawKeep`/`redrawAgain`). Don't confuse `rerollsLeft` (redraw, kept)
with `mulligansLeft` (removed).

## Backend
- `gameLogic.js`: deleted the `rerollStartingRune` method; removed the `mulligansLeft` field (player
  init + the `_resetToLobby` reset + the `startGame` reseed); removed `mulligansLeft` from getState;
  updated the startGame + completeReveal comments.
- `server.js`: removed the `reroll_starting_rune` socket handler; reworded the reveal-window comment.

## Frontend
- `game-items.js`: removed the `rerollStartingRune` emitter.
- `game-socket.js` `updateRevealRunes`: now builds the dealt-in cards only — no `onclick`, no
  `.rune-reroll-cue`, and the `.reveal-reroll-tray` (pips + counter) markup is gone.
- `styles.css`: removed the dead reroll UI (`.reveal-reroll-tray`, `.reveal-pips`/`.reveal-pip`(+lit/
  spent), `.reveal-mulligan-count`, `.rune-reroll-cue`, `.reveal-rune-card` hover-ring + `.reroll-spent`
  + `.rune-rerolled`, `@keyframes runeReroll`/`revealPipPulse`); kept the card deal-in (`runeDealIn`)
  and trimmed the reduced-motion block. Updated the section comment.
- `game-tutorial.js` + `rules.md`: "Pre-game mulligan / re-roll up to 3" → "Pre-game reveal; the hand
  you're dealt is final."

## Tests
- `test_game.js` section 102 rewritten: asserts `rerollStartingRune` is `undefined`, players have no
  `mulligansLeft`, getState ships no `mulligansLeft`, and `completeReveal` keeps the dealt hand.

## Verify
- Backend suite **992 passed / 0 failed**. `node --check` clean on all changed files.
- grep: no stray `rerollStartingRune`/`reroll_starting_rune`/`mulligansLeft`/mulligan refs (only the
  intentional removal-assertions in test 102).
- Live DOM: `rerollStartingRune` undefined; `updateRevealRunes` renders 7 display-only cards (no
  onclick, no reroll cue); removed CSS classes have zero rules; no console errors.
## Review caught a real miss (fixed)
The adversarial review workflow (9 agents) flagged that I'd overlooked `_attachRevealCountdown` in
game-socket.js — the reveal **countdown still showed live text "Re-roll your runes if you like… starting
in N"** (lines 501 + 509) plus a stale comment, and the reveal cards still *looked* clickable (base
`.roll-chip` hover lift/cursor). Fixed: countdown copy → **"Memorize the board… starting in N"** (and
the static placeholder + function comment); `.reveal-rune-card { pointer-events: none; cursor: default }`
so the display-only cards are inert. (NOTE: the two countdown strings use `…` escapes — a
`replace_all` only caught one; had to fix the second by its exact escaped form. game-socket.js is one of
the `\uXXXX`-escape files.) Re-verified live: countdown = "Memorize the board… starting in 15" (no
reroll), card `pointer-events: none`, no console errors. The review also CONFIRMED the in-game redraw
feature is fully intact. Net: 1 real issue (the countdown), now resolved.
