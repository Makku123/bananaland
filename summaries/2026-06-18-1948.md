# Run summary — 2026-06-18 19:48 PT

Audited the **farm auction** system (found it correct **and** already
well-covered), then fixed a real **bomb-tree cost config bug** that Mark signed
off on, with a regression test.

## Farm auction audit — 0 bugs
Drove the public auction API with a throwaway harness (since deleted), **33
checks, all passing:**
- **Pitch → Respond → Silent:** 0-accept → lander buys at pitch; 1-accept → that
  player buys at pitch; 2+-accept → silent tie-break where the **highest top-up
  wins and pays pitch+top-up**, and a **tied top-up leaves the farm with the
  lander** at the pitch price.
- **Buy Now** = richest **opponent** + 1, only for a strictly-richest lander;
  **ghosts excluded** from the pricing; non-richest lander → no Buy Now.
- **Pitch cap** = richest opponent's money (can pitch exactly the cap, not above).
- **Broke lander → sealed bid:** highest sealed bid wins; **tie → lander gets the
  farm**.
- **Banana ledger conserved** across every resolution path.

This all matches rules.md, and the existing suite already covers it thoroughly
(sections 10c, 72, 85i, 86, 94, 95), so **no new auction tests were added** to
avoid duplication.

## Bug fixed — bomb-tree cost couldn't be set to 0 (Mark-approved)
`this.bombCost = Math.floor(bombCost) || default` treated a configured **0** as
falsy and silently reverted to the default (1600 classic / 1000 team), so a
cheap/free bomb tree was unconfigurable. Asked Mark; he chose **"honor low costs
but clamp the floor to 1"** (never literally free).

- **`backend/gameLogic.js`** — both parse sites (constructor + lobby
  `updateSettings`) rewritten to `Number.isFinite(Math.floor(Number(x))) ? … :
  default` with a **min clamp of 1** (was 0). Now: `0 → 1`, `50 → 50`, `-10 → 1`,
  blank/NaN → default, `>99999 → 99999`.
- **`rules.md`** — the bomb-tree line claimed "range **0**–99999," but 0 was never
  actually honored (the doc was internally inconsistent). Corrected to "range
  **1**–99999" to match the implemented floor.
- **`backend/test_game.js`** — new **section 100** (100a constructor path, 100b
  lobby `updateSettings` path), **+11 assertions**.

## Verification
- **Full suite: 1232 passed, 0 failed** (was 1221 — +11), **deterministic ×2**.
- `node --check` clean on `gameLogic.js` and `test_game.js`; CRLF preserved.
- Throwaway harness ran under `/tmp` and was deleted; no repo residue.

## Process notes
- This was the run's "unsure" call — surfaced via the AskUserQuestion poll (which
  worked this time, after failing earlier in the session) rather than guessed.
- rules.md treated as intended design, not infallible: it *itself* was
  inconsistent here (claimed 0 allowed). Since Mark had just set the floor to 1,
  aligning the doc was unambiguous.

No git operations.
