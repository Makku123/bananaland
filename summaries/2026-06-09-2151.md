# Session summary — 2026-06-09 21:51 PT (interactive, Mark's rulings)

Mark answered the four standing questions via poll; this session implements
all four rulings. No git operations.

## Rulings & what changed

**1. Desert tile — "grows nothing, but still acts as a buyable farm."**
Code already did this; rules.md was wrong. Updated rules.md (board section)
to describe the desert as a buyable 0-yield farm that auctions like any
farm but never grows.

**2. Sell-listing system — remove it.** Fully removed:

- gameLogic.js: `sellProperty`/`buySale`/`cancelSale`, `sellListings` +
  `_sellListingId` state, getState entry.
- server.js: `sell_property`, `buy_sale`, `cancel_sale` handlers and the
  `sale_completed` emit.
- game.js: entire sell panel / numpad / listings panel / badge section
  (~240 lines), `sale_completed` handler, sale-flash applier, sell-button
  and listings-toggle render logic.
- board.js: sell-mode tile highlighting (two blocks), click dispatch,
  `updateSellHighlights`, the draggable listings-panel IIFE,
  `_pendingSaleFlash` reset.
- index.html: sell panel, sell button, listings toggle + panel, the
  "Sell Listings" row in How-to-Play help.
- styles.css: trade-deals toggle/panel/badge/card, btn-deal, sale-flash
  keyframes, trade-chip/phase-hint/selected/clickable tile-highlight
  blocks (~430 lines). Kept `sell-price-*`/`sell-calc-*`/`trade-panel`
  classes — the Send Bananas panel shares them.
- test_game.js: sections 27, 28, 47, 48 removed.
- TECHNICAL.md socket-event list updated.

**3. 2v2 poker crowd rule — no poker when 2+ players share the tile,
teammate or not.** `_processLanding` now counts ALL co-located live
on-board players toward the crowd; poker fires only when exactly one other
player is there and they're a non-teammate with bananas. New test 65g
covers crowd-blocks-poker + lone-enemy control case. rules.md poker
section clarified ("teammates included").

**4. Ghost sealed-bid tie — ghost keeps the tile free.** Confirmed as
intended; documented in rules.md (Leavers section). No code change.

## How it was tested

Game not running on host → sandbox. Mount again corrupted reads
(test_game.js truncated mid-line at the tail; NUL padding on others), so
every copied file was fingerprint-checked against the host (token counts
via host-side search vs sandbox grep) and the test-file tail re-spliced
from host reads.

- `node --check`: gameLogic.js, server.js, test_game.js, game.js, board.js
  all pass.
- Full suite: **504 passed / 0 failed** (520 before this session's
  removals; −18 sell asserts, +2 new crowd asserts).
- Server boot + HTTP smoke: `/`, `/index.html`, `/game.js`, `/board.js`,
  `/styles.css` all 200.
- index.html tag-balance parse: clean. styles.css braces: balanced
  (2067/2067).

## Pending / notes

- **Stale tutorial slide** (game.js "Auctions" tutorial, ~line 6770): it
  describes bidding on *owned* farms with owner accept/reject — that
  mechanic doesn't exist. Should be rewritten to describe the real
  pitch → respond → silent-tiebreak auction. Left for a future run.
- 10s ghost-concede grace window still awaits Mark's explicit confirmation
  (behavior unchanged).
- Mount corruption persists (lossy/duplicating reads of large files
  through the mount). Host files verified intact via file tools.
- Mark should restart the backend server to pick up the changes.
