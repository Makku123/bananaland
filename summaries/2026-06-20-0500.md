# Session 2026-06-20 0500 — Magic Dice rename + Random Start to board center + Banana Gadget

Three user requests.

## 1. "Guaranteed Roll cards" → "Magic Dice" (rename + re-theme)
- USER-FACING rename everywhere: "Guaranteed Roll(s)"/"guaranteed-roll card" → **Magic Dice** /
  "a magic die"; **Cancel Guaranteed Roll** → **Cancel Magic Die**. Applied across gameLogic.js +
  server.js (logs/comments), game-items.js, game-screen.js, game-socket.js, board.js, game-tutorial.js,
  index.html, styles.css, rules.md (counted Node scripts + targeted edits; "card"→"die"/"dice" only
  where it refers to a magic die, never poker cards).
- INTERNAL identifiers KEPT (convention): `rollCards`/`useRollCard`/`armRollCard`/`cancelItems`, sockets
  `use_roll_card`/`cancel_items`, CSS classes `roll-chip*`/`roll-play-card*`/`mini-roll-card*`. ONLY the
  panel container DOM id/class was renamed `guaranteed-rolls`→`magic-dice` (HTML + JS + CSS in sync).
- Visual re-theme ("die-styled, keep numbers"): `.roll-chip` is now a **square 54×54 die** with a cube
  bevel (was a tall card); `.roll-play-card` (played-move) and `.mini-roll-card` (profile) are square
  dice too — all keep the big 1–6 numeral.
- Also fixed STALE tutorial content (game-tutorial.js) that still described the removed revealed-cards /
  card-auction mechanic — rewrote the Magic Dice + Super Banana steps to the current "concealed-only +
  SB grants one random die" model.

## 2. Random Start button → middle of the board
- Moved `#btn-random-start` out of the action panel into a centered `#board-random-start` overlay inside
  `#board` (`.board-center-prompt`, absolute + translate(-50%,-50%)), with a "Pick your starting tile"
  prompt. game-screen.js toggles it on the viewer's start-pick turn.
- **Gotcha:** board.js `renderBoard` strips all non-pile children of `#board` and re-appends a fixed
  allow-list — added `boardRandomStart` to that list (board.js ~586 + ~634) so it survives re-renders.

## 3. Banana Gadget
- New action-panel gadget (`#banana-gadget`): a 1–12 slider that live-previews the sum of the viewer's
  OWN banana piles on the tiles they'd cross/land traveling that many tiles from their current position
  (revealed tiles only). Pure frontend: `setBananaGadget` / `_bananaGadgetTotal` / `updateBananaGadget`
  in game-screen.js (recomputed every frame). Matches `_collectBananasOnPath`'s own-pile rule.

## Verify
- `node backend/test_game.js` → **1012 passed, 0 failed** (rename didn't touch any assertion). Harnesses
  43/43 + 20/20.
- **Live (preview, 3-player socket game):** Random Start button **centered** in board during start-pick
  (boardCenter == overlayCenter); Magic Dice panel + **square dice chips** (1/4/5/6); Banana Gadget
  shows "Travel N → collect X🍌 from your farms" and computed correctly (injected piles: tile 1=50,
  tile 5=30 → 50 at N=1, 80 at N=5; counts only own piles, position-gated, fog-safe). Zero console errors.
  (Had to reload the stale preview tab to pick up new HTML.)
- Adversarial review workflow (rename-completeness / gadget-logic / random-start+restyle / completeness
  critic) run as a final sweep.
