# Session 2026-06-20 0830 — per-rune mulligan + (verified) start-draw animation, first-finder pick, vertical rune cards

The user's batch: rename Magic Dice→Magic Runes (already done last session), a pre-game mulligan, a
start-of-game rune draw animation, the first SB finder picks 1–6, and vertical rectangular rune cards.

## Key finding
Most of this batch was **already implemented in the working tree** (parallel "user/linter" edits):
the whole-hand mulligan, the start-of-game rune **draw animation** (staggered deal on the reveal
screen), the **first-finder PICK** (`_sbDiscovered` + `_grantMagicRunePick` + `pendingPick` + pick-mode
redraw session + `redrawPick` + a 1–6 picker modal), and the **vertical rectangular** card shapes
(`.roll-chip` 46×66, `.roll-play-card`, `.mini-roll-card`) — all tested (section 102) and passing.

So the only spec gap was the mulligan's form. Net change this session:

## Mulligan: whole-hand ×1 → individual ×3 (per the answer to my clarifying question)
- Backend: replaced `mulliganRollCards(socketId)` (whole hand, once) with **`rerollStartingRune(socketId,
  index)`** — re-rolls ONE rune, decrements a **`mulligansLeft` budget of 3** (was `mulliganUsed` bool).
  Guards: state "revealing", own hand, budget>0, in-range index. Seeded to 3 in startGame / addPlayer /
  _resetToLobby; shipped owner-only in getState. Socket `mulligan_runes` → **`reroll_starting_rune
  {index}`**.
- Frontend: the reveal-runes screen's whole-hand button → **each rune card is tappable to re-roll it**,
  with a "🔄 Tap a rune to re-roll it — N left" counter (3→0) and a per-rune pop animation (only the
  changed rune re-animates; the staggered deal-in on first show is kept). `.reveal-mulligan-btn` CSS
  removed; added `.rune-rerolled` pop + `.reroll-spent`.
- Tests: section 102 mulligan tests rewritten for per-rune ×3 (budget decrement, out-of-range rejected,
  same-slot re-roll OK, exhausted budget, none once playing).
- Docs: rules.md + tutorial wording updated.

## Verify
- `node backend/test_game.js` → **1064 passed, 0 failed**. Harnesses 43/43 + 20/20.
- **Live (preview, reveal phase):** 9 runes seeded with **3 re-rolls**; counter "…3 left"; a re-roll
  decrements to 2; after 3 the **4th is rejected (stays 0)**; rune card renders **vertical** (35×44).
  Zero console errors. (Restarted the preview server so the backend change loaded.)
- Adversarial review (Explore agent) of the mulligan conversion: **clean, no findings** — all guards
  present, `mulligansLeft` owner-only, no dangling old names (`mulliganRollCards`/`mulligan_runes`/
  `mulliganUsed`/`mulliganRunes`/`reveal-mulligan-btn`), adjacent first-finder-pick + redraw intact.

## Notes
- Rune chips were already vertical cards (no change needed) and the draw animation + first-finder pick
  were already in place — verified rather than rebuilt.
- Confirmed the earlier preview gotcha again: backend edits need a preview stop+start (reused process
  runs stale backend).
